/**
 * kaltura-upload directive.
 */
angular.module('kaltura-upload', []).directive('kalturaUpload', function () {
    return {
        restrict: "A,E",
        replace: false,
        transclude: false,
        compile: function (element, attrs) {

            // implement

            return function ($scope, element, attrs, controller) {

                // implement

            };

        }
    };
});
